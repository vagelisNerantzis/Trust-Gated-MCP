name: trust-gated-mcp
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy>=1.24
  - pandas>=2.0
  - matplotlib>=3.7
  - pydantic>=2.0
  - pip
  - pip:
    - llama-cpp-python>=0.2.80
    - scipy
    - langchain
    - langchain-core
    - langchain-ollama
    - mcp
    - pydantic
